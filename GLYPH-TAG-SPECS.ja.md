# Glyph Tag Specification (仕様書)
Author: jp-rad  
Last Updated: 2026‑09‑03  


# 1. Purpose（目的）

Glyph Tag は、**文字図形名（glyph-name）** を中心として
字形情報を表現するための抽象タグ形式です。

文字図形名は、特定の字形体系における **文字図形（glyph）を識別するための抽象名** であり、
MJ文字図形名、GJ文字図形名など体系固有の名称を含みます。

1. 文字図形名を「基本文字コード」と「実装文字コード」に対応付ける抽象レイヤを提供します

2. 異なる字形体系を統一的に扱うための共通フォーマットを提供します

3. 正規化（Normalization）と描画（Rendering）を分離した処理モデルを提供します


**基本文字コード（Basic Character Code）** は  
Unicode の標準的なコードポイントです。

**実装文字コード（Implementation Character Code）** は  
実装環境が採用する字形を表すコードポイントです。  
Unicode 標準のコードポイントに加え、  
IVS（Ideographic Variation Sequence：表意文字異体字シーケンス）や  
PUP（Private Use Plane：私用面コードポイント）も含みます。


# 2. Syntax（構文）

Glyph Tag の構文を次に示します。

```
{<glyph-name> [b=<UCSSeq>] [v=<UCSSeq>] [set=<Identifier>]}
```


## 2.1 構文要素

### A. `<glyph-name>` — 文字図形名（必須）

### B. `b=<UCSSeq>` — 基本文字コード（任意）

### C. `v=<UCSSeq>` — 実装文字コード（任意）

### D. `set=<Identifier>` — 字形体系識別子（任意）


## 2.2 属性の順序

属性の順序は任意です。


## 2.3 属性の省略

省略された属性は正規化フェーズによって決定されます。


## 2.4 UCSSeq の構文

```
U+XXXX
U+XXXX U+YYYY
U+XXXX U+YYYY U+ZZZZ
```


# 3. UCSSeq（UCS コードポイント並び）

UCSSeq は Unicode のコードポイントを空白区切りで並べたものです。

- 基本文字コード  
- 実装文字コード（Unicode 標準コードポイント、IVS、PUP）


# 4. Attributes（タグと属性）

## 4.1 glyph-name — 文字図形名

字形体系における文字図形を識別する抽象名です。  
MJ文字図形名、GJ文字図形名など体系固有の名称を含みます。


## 4.2 b — base（基本字形）

基本文字コードを表します。  
IVS を含まない UCSSeq です。


## 4.3 v — variant（実装文字コード）

実装文字コードを表します。  
IVS や PUP を含む場合がありますが、  
異体字に限らず、実装環境が採用する字形を表すコードポイント全般です。


## 4.4 set — Glyph System Identifier

字形体系の識別子です。  
正規化後は環境の set に置き換えられます。


# 5. Escape Rules（エスケープ規則）

本仕様では、制御文字（`{`）やエスケープ文字（`\`）の特殊な意味を打ち消し、  
プレーンテキスト（地文）としてそのまま描画・表示するために、  
**バックスラッシュ（`\`）を用いたエスケープ規則**を採用します。


## 5.1 中カッコの表示（ `\{` ）

Glyph Tag の開始記号 `{` を文字列としてそのまま表示したい場合は、  
開始記号の直前にバックスラッシュを付与して `\{` と記述します。

入力例:

```
現在の文字は \{MJ090001} です。
```

描画結果:

```
現在の文字は {MJ090001} です。
```


## 5.2 バックスラッシュの表示（ `\\` ）

プレーンテキスト中にバックスラッシュ記号そのものを 1文字として表示したい場合は、  
バックスラッシュを 2 回重ねて `\\` と記述します。

入力例:

```
エスケープするには \\\{MJ090001} と記述してください。
```

描画結果:

```
エスケープするには \{MJ090001} と記述してください。
```



## 5.3 処理系における位置づけ

解析（パース）処理において、  
エスケープされた要素（`\{` および `\\`）は  
Glyph Tag トークンとしての抽出対象から除外されます。

これらは正規化フェーズの間はそのまま保持され、  
描画フェーズにおいてのみエスケープが解除されます。

解除規則:

```
\{  → {
\\  → \
```


# 6. Normalization（正規化）

正規化は、Glyph Tag を  
**環境固有の字形体系に従って再解釈し、  
省略された属性を決定する処理**です。

## 6.1 正規化の目的

- 文字図形名を環境固有の字形体系に照合します  
- b/v を環境固有の UCSSeq に置換します  
- set を環境の set に置き換えます  
- 省略された属性を決定します

## 6.2 正規化の流れ

1. エスケープされた要素（`\{`, `\\`）を保持します  
2. Glyph Tag を抽出します  
3. 文字図形名を Glyph Table に照合します  
4. 省略された属性を決定します  
5. エスケープ要素は描画フェーズまで保持します



# 7. Rendering（描画）

描画フェーズは、正規化済みの Glyph Tag を  
実際の UCS 文字列へ展開する処理です。

## 7.1 描画モード

```
mode="v"       → v → b → 豆腐文字
mode="b"       → b → 豆腐文字
```

## 7.2 豆腐文字（tofu）

v/b が存在しない場合に使用する文字コードです。  
例：U+25A1（□）

## 7.3 エスケープ解除

```
\{  → {
\\  → \
```


# Annex（補足資料）


## Annex A — Unicode IVS / IVD（国際標準）

- IVS は ISO/IEC 10646 に規定されています。  
- IVD は Unicode Consortium が管理する異体字データベースです。  
- Moji_Joho IVD Collection は日本政府の公式コレクションです。  
- Glyph Tag の b/v モデルは IVS と整合します。


## Annex B — MJ文字（Moji_Joho IVD）

MJ文字は Unicode に符号化済みであり、  
必要に応じて IVS を持ちます。

例：

```
MJ022336: { b: "U+845B", v: "U+845B U+E0103" }
MJ022335: { b: "U+845B", v: "U+845B U+E0102" }
```


## Annex C — 行政事務標準文字（MJ＋GJ）

行政事務標準文字は次で構成されます。

- MJ文字（Unicode符号化済み）  
- GJ文字（未符号化 → PUP）


## Annex D — GJ文字（暫定私用面）

GJ文字は Unicode 未符号化のため、 Glyph Tag では次の 2属性で表現します。

### b — GJ代替文字（fallback glyph）

意味的に近い UCS 文字や縮退代替文字を設定します。

### v — GJ暫定私用コード（PUP）

例：

```
v = "U+100ABC"
```


## Annex E — フォント実装

MJ文字やGJ文字を含んだフォントが[文字情報技術促進協議会](https://moji.or.jp/)や[デジタル広域推進機構](https://www.digitalwidearea.org/)から配布されています。

### IPAMJ明朝  
MJ文字の UCS/IVS が実装されています。  
https://moji.or.jp/mojikiban/font/


### DWPI明朝  
行政事務標準文字（MJ＋GJ）が実装されています。  
https://www.digitalwidearea.org/dwpi_mincho#h.23rvyj1hq9jd


### DWPIex明朝  
変体かな濁点の IVS 割り当てなど が実装されています。  
https://www.digitalwidearea.org/dwpi_mincho#h.23rvyj1hq9jd


## Annex F — レガシー外字システムとのデータ連携

Glyph Tag はレガシー外字体系を統一的に扱えます。

- レガシー外字 → 文字図形名（MJ文字図形名、GJ文字図形名、もしくは、独自の文字図形名）  
- 外字グリフ → b/v にマッピング  
- 外字フォントが失われても字形情報を保持できます
